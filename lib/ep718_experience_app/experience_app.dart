import 'package:flutter/material.dart';

import 'experience_home_page.dart';

class ExperienceApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: ExperienceHomePage(),
    );
  }
}
